template(name="header")
  .navabar.navbar-default.navbar-static-top(role="navigation")
    .container
      .navbar-header
        button.navbar-toggle.collapsed(type="button", data-toggle="collapse", data-target=".navbar-collapse")
          span.sr-only Toggle navigation
          span.icon-bar
          span.icon-bar
          span.icon-bar
        a.navbar-brand(href="{{ pathFor 'home' }}") StatCoach

      .navbar-collapse.collapse
        ul.nav.navbar-nav.navbar-right
          li
            a(href="{{ pathFor 'team' _id=userTeam._id }}")
              strong Team
              | {{ userTeam.name }}
          +loginButtons
